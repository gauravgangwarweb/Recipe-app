
<tr id="<%= dom_id(food) %>">
  <td><%= food.name %></td>
  <td><%= food.measurement_unit %></td>
  <td>$<%= food.price %></td>
  <td><%= link_to "Delete", food, method: :delete, data: { confirm: "Are you sure?" }, class: "border left" %></td>
</tr>

